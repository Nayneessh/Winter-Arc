# Supabase client configuration for Winter Arc.
#
# THESE VALUES ARE SAFE TO COMMIT. The publishable (anon) key is designed to ship inside
# client applications — it is present in every browser and mobile build of every Supabase
# app. It grants no authority by itself: access is decided entirely by row-level security
# policies, which scope every row to the authenticated user (see
# supabase/migrations/20260904000100_initial_schema.sql).
#
# The SERVICE ROLE key bypasses row-level security. It is not here, is not in the APK, and
# must never be committed. Anything genuinely secret belongs in secrets.properties, which is
# git-ignored, or in a CI secret.
#
# Overridden by the SUPABASE_URL / SUPABASE_ANON_KEY environment variables when set.
SUPABASE_URL=https://bwnruvqapzzygmhtgejb.supabase.co
SUPABASE_ANON_KEY=sb_publishable_j7PVOkY1eDEBe3tosdZdBQ_ismVr9kU
